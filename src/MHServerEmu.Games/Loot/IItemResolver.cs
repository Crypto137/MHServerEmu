namespace MHServerEmu.Games.Loot
{
    /// <summary>
    /// An interface for implementing the visitor pattern used in loot tables (separate from LootTableNodeVisitor).
    /// </summary>
    public interface IItemResolver
    {
    }
}
